on:
  workflow_dispatch:
    inputs:
      string:
        description: 'Input test string'
        required: true
        type: string
      json:
        description: 'Input test json'
        required: true
        type: string

# env:
#   ACTIONS_ALLOW_UNSECURE_COMMANDS: true

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Set and mask variable
        run: |
          JSON_TEST=$(jq -r '.inputs.json' $GITHUB_EVENT_PATH)
          echo "::set-env name=JSON_TEST::$JSON_TEST"
          JSON_VALUE=$(echo "$JSON_TEST" | jq -r '.test')
          echo "::add-mask::$JSON_VALUE"

        # Working (new way of setting variable)
        #   JSON_TEST=$(jq -r '.inputs.json' $GITHUB_EVENT_PATH)
        #   STRING_TEST=$(jq -r '.inputs.test' $GITHUB_EVENT_PATH)
        #   echo "JSON test value before mask: $JSON_TEST"
        #   echo "::set-env name=JSON_TEST::$JSON_TEST"
        #   JSON_VALUE=$(echo "$JSON_TEST" | jq -r '.test')
        #   echo $JSON_VALUE
        #   echo "::add-mask::$JSON_VALUE"
        #   echo $JSON_VALUE
        

        # Working (old way of seting variable)
        #   JSON_TEST=$(jq -r '.inputs.json' $GITHUB_EVENT_PATH)
        #   STRING_TEST=$(jq -r '.inputs.test' $GITHUB_EVENT_PATH)
        #   echo "JSON test value before mask: $JSON_TEST"
        #   echo "JSON_TEST=$JSON_TEST" >> $GITHUB_ENV
        #   JSON_VALUE=$(echo "$JSON_TEST" | jq -r '.test')
        #   echo $JSON_VALUE
        #   echo "::add-mask::$JSON_VALUE"
        #   echo $JSON_VALUE

        # old commands
        #   echo "${{ env.JSON_TEST }}"
        #   echo "JSON_TEST=$JSON_TEST" >> $GITHUB_ENV
        #   echo "Test value before mask: $TEST"
        #   echo "::add-mask::$JSON_TEST"
        #   echo "::add-mask::$TEST"
        #   echo "JSON test value after mask: $JSON_TEST"
        #   echo "Test value after mask: $TEST"
        #   echo "TEST=$TEST" >> $GITHUB_ENV

        #   JSON_VALUE=${{ fromJson(env.JSON_TEST).test }}
        #   echo $JSON_VALUE
        #   echo "JSON_VALUE=${{ fromJson(env.JSON_TEST).test }}" >> $GITHUB_ENV
        #   echo "${{ env.JSON_TEST }}"
        #   echo "${{ env.TEST }}"
        #   echo "${{ env.JSON_VALUE }}"

#  set the input as a var using fromjsonloads syntax
#  mask the var created
#  call an action passing the masked var
#  have the action
#    setup go
#    run a go script using the masked var
#   echo "ENV_VAR=${{ fromJson(inputs.test-json).test }}" >> $GITHUB_ENV